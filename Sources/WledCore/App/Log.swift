import Foundation
import os

public enum Log {
    public static let app = Logger(subsystem: "io.wledcast.native", category: "app")
    public static let permissions = Logger(subsystem: "io.wledcast.native", category: "permissions")
    public static let discovery = Logger(subsystem: "io.wledcast.native", category: "discovery")
    public static let session = Logger(subsystem: "io.wledcast.native", category: "session")
    public static let transport = Logger(subsystem: "io.wledcast.native", category: "transport")
    public static let capture = Logger(subsystem: "io.wledcast.native", category: "capture")
}
import Foundation
import os

public enum Log {
    public static let app = Logger(subsystem: "io.wledcast.native", category: "app")
    public static let permissions = Logger(subsystem: "io.wledcast.native", category: "permissions")
    public static let discovery = Logger(subsystem: "io.wledcast.native", category: "discovery")
    public static let session = Logger(subsystem: "io.wledcast.native", category: "session")
    public static let transport = Logger(subsystem: "io.wledcast.native", category: "transport")
    public static let capture = Logger(subsystem: "io.wledcast.native", category: "capture")
}

